Mind Map generated by NB MindMap plugin
> __version__=`1.1`,showJumps=`true`
---

# Root topic

## React\-ECS

### Transform/Rect/Root

#### 顶层组件

#### \<Transform\> 内部可以包含资深MonoBehaviour组件或者React\.Component

#### 当组件包含 Name或者ClassName时,从Children里面查找匹配

#### 当一个组件的Bind类型是Monobehaviour时, 它是一个绑定的Monobehaviour组件, <br/>否则它是React\.Component,仅用于状态管理

#### 默认target=childrenAndSelf, 可以明确指定 target=self

### GameObejct=Entity

#### 作为后端Entity使用, 不在React写法中出现

#### React所有组件操作均从顶层Transform/Rect/World开始匹配

#### Prefab

##### 表示方式为：\<Transform asset=\{prefab\}\>

#### Scene

##### 表示方式为 \<World asset= \{scene\}\>

#### Entity\.Add\<Button name=\{name\} @class=\{className\}\>

##### Entity\.Remove

###### 添加和删除组件或者Monobehaviour

#### Entity\.Create/Entity\.Update/Entity\.Render/Entity\.Destory

##### GameObject Hierarchy 操作

#### Entity\.Where\(\)\.Any\(\)\.All\(\)\.Not\(\)\.ForEach\(\)

#### Enity\.Find\("\[name=test\]\>button"\)

### Systems=Command=React\.Component

#### class Command extends React\.Component \{\}

#### TheSystem extends Command \{\}

#### render\(\) =\> \<Observer/\>

### World=Scene

### Event=状态管理=react\-mobx\-lite

### LoadAsset=require\(\)/import form 'xxx\.asset'
